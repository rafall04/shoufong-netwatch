# Database
DATABASE_URL="file:./devicemap.db"

# NextAuth.js v5
AUTH_SECRET="your-secret-key-here-change-in-production"
AUTH_TRUST_HOST=true
# Generate with: openssl rand -base64 32

# MikroTik Configuration (configured via UI, not env vars)
# These are stored in the SystemConfig table
